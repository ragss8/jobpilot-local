# Core web app and matching need only Python 3.11+.
# PDF / DOCX import and export:
pypdf==6.10.0
python-docx==1.2.0
reportlab==4.4.9
# Optional local browser automation (also install Chromium):
playwright==1.58.0
# IANA timezones on Windows:
tzdata>=2025.2,<2027
